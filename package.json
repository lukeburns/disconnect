{
  "name": "disconnect",
  "description": "Connect middleware disconnected from http.",
  "version": "3.0.1",
  "author": "Luke Burns <lukemburns@gmail.com> (http://lukeburns.com)",
  "keywords": [
    "middleware",
    "connect",
    "disconnect",
    "rack"
  ],
  "repository": "lukeburns/disconnect",
  "dependencies": {
    "debug": "1.0.2",
    "utils-merge": "1.0.0"
  },
  "devDependencies": {
    "istanbul": "0.2.14",
    "mocha": "~1.20.0",
    "should": "~4.0.0",
    "supertest": "~0.13.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">= 0.10.0"
  },
  "scripts": {
    "test": "mocha --require test/support/env --reporter dot --check-leaks test/",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --require test/support/env --reporter dot --check-leaks test/",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --require test/support/env --reporter spec --check-leaks test/"
  }
}
